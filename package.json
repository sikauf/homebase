{
  "name": "homebase",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "test": "npm test -w client && npm test -w server",
    "push-saves": "node scripts/push-saves.mjs",
    "deploy": "fly deploy"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
